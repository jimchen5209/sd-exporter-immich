[project]
name = "sd-exporter-immich"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lxml>=6.0.2",
    "sd-prompt-reader>=1.3.5",
    "tqdm>=4.67.1",
]

[dependency-groups]
dev = [
    "types-lxml>=2026.1.1",
]
